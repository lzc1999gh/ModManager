<Application x:Class="ModManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="ThemeBorderBrush" Color="#202020" />
        <SolidColorBrush x:Key="ThemeBorderStrongBrush" Color="#8B0000" />
        <SolidColorBrush x:Key="ThemeAccentBrush" Color="#B00020" />
        <SolidColorBrush x:Key="ThemeAccentBrightBrush" Color="#D00000" />
        <SolidColorBrush x:Key="ThemeTextBrush" Color="#111111" />
        <SolidColorBrush x:Key="ThemeMutedTextBrush" Color="#333333" />

        <!-- 主区域统一外框，保证四个区域的最上、最下边界一致。 -->
        <Style x:Key="SectionBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{StaticResource ThemeBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <!-- 标题下内容区统一使用的边框。 -->
        <Style x:Key="SectionContentBorderStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{StaticResource ThemeBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" />
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <Style TargetType="Control">
            <Setter Property="FontFamily" Value="Microsoft YaHei UI, Microsoft YaHei, Segoe UI" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Microsoft YaHei UI, Microsoft YaHei, Segoe UI" />
            <Setter Property="Foreground" Value="{StaticResource ThemeTextBrush}" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource ThemeTextBrush}" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource ThemeTextBrush}" />
            <Setter Property="CaretBrush" Value="{StaticResource ThemeAccentBrush}" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Foreground" Value="{StaticResource ThemeTextBrush}" />
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource ThemeTextBrush}" />
        </Style>
    </Application.Resources>
</Application>
