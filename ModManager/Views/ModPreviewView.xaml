<UserControl x:Class="WpfApp1.Views.ModPreviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:conv="clr-namespace:WpfApp1.Converters"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:ImagePathToImageSourceConverter x:Key="ImagePathToImageSource" />
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Text="Mod 预览" FontWeight="Bold" Margin="4" />
            <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="6" Margin="8" Width="260" Height="160" Background="#111">
                <Image Source="{Binding SelectedMod.PreviewPath, Converter={StaticResource ImagePathToImageSource}}" Stretch="Uniform" />
            </Border>
            <TextBlock Text="{Binding SelectedMod.FilePath}" FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="4"/>
        </StackPanel>
    </Grid>
</UserControl>
