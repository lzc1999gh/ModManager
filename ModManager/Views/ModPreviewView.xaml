<UserControl x:Class="ModManager.Views.ModPreviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:conv="clr-namespace:ModManager.Converters"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:ImagePathToImageSourceConverter x:Key="ImagePathToImageSource" />
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Text="Mod 预览" FontWeight="Bold" Margin="4" />
            <Image Source="{Binding SelectedMod.PreviewPath, Converter={StaticResource ImagePathToImageSource}}" Stretch="Uniform" Margin="2"/>
            <TextBlock Text="{Binding SelectedMod.FilePath}" FontSize="11" Foreground="Gray" TextWrapping="Wrap" Margin="4"/>
        </StackPanel>
    </Grid>
</UserControl>
