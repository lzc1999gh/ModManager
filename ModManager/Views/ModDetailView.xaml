<UserControl x:Class="WpfApp1.Views.ModDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:xctk="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
    mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <TextBlock Text="Mod 详情 &amp; INI 快捷键" FontWeight="Bold" Margin="4" />
            <TextBlock Text="{Binding SelectedMod.Name}" FontSize="14" FontWeight="SemiBold" Margin="4"/>
            <TextBlock Text="INI 快捷键:" Margin="4"/>
            <ListBox ItemsSource="{Binding SelectedMod.IniShortcuts}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="4">
                            <TextBlock Text="{Binding Key}" Width="120" VerticalAlignment="Center"/>
                            <xctk:IntegerUpDown Value="{Binding Value, Mode=TwoWay}" Width="80" Minimum="0" Maximum="999" Margin="8,0"/>
                            <ComboBox SelectedIndex="{Binding OptionIndex, Mode=TwoWay}" Width="80">
                                <ComboBoxItem>0</ComboBoxItem>
                                <ComboBoxItem>1</ComboBoxItem>
                                <ComboBoxItem>2</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
