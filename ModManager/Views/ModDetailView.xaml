<UserControl x:Class="ModManager.Views.ModDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="ImageButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#A0A0A0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Right" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#3399FF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E8F3FF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="4" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="5" Padding="8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Mod 详情" FontWeight="Bold" Margin="1,0,0,0"/>
                <Button Grid.Row="0" Grid.Column="2" Command="{Binding DeleteModCommand}" Style="{StaticResource ImageButtonStyle}" ToolTip="删除当前 Mod" FontSize="15" Foreground="#C62828" Background="Transparent" >
                    <svg:SvgViewbox Width="20" Height="20" Source="pack://siteoforigin:,,,/Resources/Icons/del.svg" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Button>
                <Button Grid.Row="1" Grid.Column="2" Command="{Binding OpenModFolderCommand}" Style="{StaticResource ImageButtonStyle}" ToolTip="打开Mod文件夹" FontSize="15" Foreground="#C62828" Background="Transparent">
                    <svg:SvgViewbox Width="20" Height="20" Source="pack://siteoforigin:,,,/Resources/Icons/open.svg" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Button>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="名称：" Margin="1,2,5,2" VerticalAlignment="Center" />
                <Grid Grid.Row="1" Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock x:Name="ModNameText"
                               Text="{Binding SelectedMod.Name}"
                               ToolTip="双击修改名称"
                               MouseLeftButtonDown="ModNameText_MouseLeftButtonDown">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedMod.IsEditingName}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Setter Property="Margin" Value="1,2,5,2" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBox x:Name="ModNameEditor"
                             Text="{Binding SelectedMod.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             LostKeyboardFocus="ModNameEditor_LostKeyboardFocus"
                             KeyDown="ModNameEditor_KeyDown">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedMod.IsEditingName}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="来源：" Margin="1,2,5,2" VerticalAlignment="Center" />
                <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                         ToolTip="可填写 Mod 下载链接、作者或来源平台"
                         TextChanged="SourceTextBox_TextChanged"
                         Text="{Binding SelectedMod.Source, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                         BorderThickness="0.5"
                         Background="Transparent"
                         >
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="4"/>
                            <Setter Property="BorderBrush" Value="#c1d0dc"/>
                            <Setter Property="BorderThickness" Value="0.5" />
                            <Setter Property="Background" Value="Transparent" />
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Grid>
        </Border>

        <Border Grid.Row="1" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="5" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0">
                    <TextBlock Text="快捷键" VerticalAlignment="Center" Margin="8,0,4,0" />
                    <StackPanel Orientation="Horizontal" >
                        <ToggleButton IsChecked="{Binding ShowIniContent, Mode=TwoWay}"
                                      Width="34" Height="20" VerticalAlignment="Center"
                                      ToolTip="关闭显示快捷键，开启显示 INI 内容" HorizontalAlignment="Center">
                            <ToggleButton.Style>
                                <Style TargetType="ToggleButton">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Grid Width="34" Height="20">
                                                    <Border x:Name="Track" Background="#A8A8A8" CornerRadius="10" />
                                                    <Ellipse x:Name="Thumb" Width="16" Height="16" Fill="White" Margin="2"
                                                             HorizontalAlignment="Left" VerticalAlignment="Center" />
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Track" Property="Background" Value="#3399FF" />
                                                        <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ToggleButton.Style>
                        </ToggleButton>
                        <Button Content="读取" Command="{Binding ReadIniCommand}" Margin="8,0,0,0" />
                        <Button Content="打开" Command="{Binding OpenIniCommand}" Margin="4,0,0,0" />
                    </StackPanel>
                </DockPanel>

                <TextBlock Grid.Row="1" Text="{Binding SelectedMod.IniFilePath}" FontSize="11" Foreground="Gray"
                           TextTrimming="CharacterEllipsis" Margin="0,6,0,0" />

                <ListBox Grid.Row="2" Grid.RowSpan="2" ItemsSource="{Binding SelectedMod.IniShortcuts}" Margin="0,6,0,0">
                    <ListBox.Style>
                        <Style TargetType="ListBox">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowIniContent}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.Style>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Key}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBox Grid.Row="2" Grid.RowSpan="2" Margin="0,6,0,0" Text="{Binding SelectedMod.IniContent}"
                         IsReadOnly="True" TextWrapping="NoWrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas" FontSize="11" >
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowIniContent}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="4"/>
                            <Setter Property="BorderBrush" Value="#c1d0dc"/>
                            <Setter Property="BorderThickness" Value="0.5" />
                            <Setter Property="Background" Value="Transparent" />
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
