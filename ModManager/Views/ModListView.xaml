<UserControl x:Class="ModManager.Views.ModListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:conv="clr-namespace:ModManager.Converters"
    xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
    mc:Ignorable="d">

    <UserControl.Resources>
        <conv:ImagePathToImageSourceConverter x:Key="ImagePathToImageSource" />

        <!-- Mod 名称悬浮提示 -->
        <Style x:Key="ModNameToolTipStyle" TargetType="ToolTip">
            <Setter Property="Placement" Value="Mouse" />
            <Setter Property="PlacementRectangle" Value="0,0,0,0" />
            <Setter Property="HorizontalOffset" Value="10" />
            <Setter Property="VerticalOffset" Value="10" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="#F8F8F8" />
            <Setter Property="Foreground" Value="#222222" />
            <Setter Property="BorderBrush" Value="#BBBBBB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="MaxWidth" Value="500" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">

                            <ContentPresenter
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ImageButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="3" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#A0A0A0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Border" Property="BorderBrush" Value="#3399FF" /></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Border" Property="Background" Value="#E8F3FF" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Orientation="Horizontal"
                Margin="6"
                VerticalAlignment="Center">

                <TextBlock
                    Text="Mod"
                    FontWeight="Bold"
                    VerticalAlignment="Center" />

                <TextBlock
                    Text="{Binding SelectedCharacter.Mods.Count, StringFormat='({0})'}"
                    VerticalAlignment="Center"
                    Margin="5,0,0,0"
                    Foreground="#33CC66" />
            </StackPanel>

            <Button
                Grid.Column="1"
                Command="{Binding RefreshModsCommand}"
                Style="{StaticResource ImageButtonStyle}"
                Margin="2"
                VerticalAlignment="Center">

                <svg:SvgViewbox
                    Width="18"
                    Height="18"
                    Source="pack://siteoforigin:,,,/Resources/Icons/re.svg" />
            </Button>
        </Grid>

        <!-- Mod 列表 -->
        <Border Grid.Row="1" BorderBrush="#D0D0D0" BorderThickness="1" CornerRadius="5" Padding="3">
            <ListBox
            Grid.Row="1"
            ItemsSource="{Binding SelectedCharacter.Mods}"
            SelectedItem="{Binding SelectedMod}"
            AllowDrop="True"
            PreviewDragOver="ListBox_PreviewDragOver"
            Drop="ListBox_Drop"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            HorizontalContentAlignment="Stretch"
            BorderThickness="0"
            >
                <ListBox.Resources>

                    <BooleanToVisibilityConverter x:Key="BoolToVis" />

                    <!-- 每个 Mod 项目 -->
                    <Style TargetType="ListBoxItem" x:Key="ModListItemStyle">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="6" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="ToolTip">
                            <Setter.Value>
                                <ToolTip Style="{StaticResource ModNameToolTipStyle}" Content="{Binding Name}" />
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border
                                    x:Name="Bd"
                                    CornerRadius="6"
                                    BorderThickness="1"
                                    BorderBrush="#000000"
                                    Background="#FFFFFF"
                                    SnapsToDevicePixels="True"
                                    HorizontalAlignment="Stretch">
                                        <Grid Height="56">
                                            <Grid.ColumnDefinitions>
                                                <!-- 名称区域 -->
                                                <ColumnDefinition Width="*" />
                                                <!-- 状态区域 -->
                                                <ColumnDefinition Width="40" />
                                            </Grid.ColumnDefinitions>
                                            <!-- 名称 -->
                                            <ContentPresenter
                                            Grid.Column="0"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Center"
                                            Margin="12,0,6,0"
                                            RecognizesAccessKey="True" />
                                            <!-- 状态切换按钮：保持右侧状态区域的原有宽度 -->
                                            <ToggleButton
                                            Grid.Column="1"
                                            Command="{Binding DataContext.ToggleModCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            IsChecked="{Binding Enabled, Mode=OneWay}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Width="34"
                                            Height="20"
                                            Cursor="Hand"
                                            ToolTip="启用 / 禁用">
                                                <ToggleButton.Style>
                                                    <Style TargetType="ToggleButton">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <Grid Width="34" Height="20">
                                                                        <Border x:Name="Track" Background="#A8A8A8" CornerRadius="10" />
                                                                        <Ellipse x:Name="Thumb" Width="16" Height="16" Fill="White" Margin="2"
                                                                             HorizontalAlignment="Left" VerticalAlignment="Center" />
                                                                    </Grid>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsChecked" Value="True">
                                                                            <Setter TargetName="Track" Property="Background" Value="#33CC66" />
                                                                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                                                        </Trigger>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="Track" Property="Opacity" Value="0.85" />
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ToggleButton.Style>
                                            </ToggleButton>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <!-- Enabled -->
                                        <DataTrigger
                                        Binding="{Binding Enabled}"
                                        Value="True">
                                            <Setter
                                            TargetName="Bd"
                                            Property="BorderBrush"
                                            Value="#33CC66" />
                                            <Setter
                                            TargetName="Bd"
                                            Property="Background"
                                            Value="#EEFFFB" />
                                        </DataTrigger>
                                        <!-- 选中 -->
                                        <Trigger
                                        Property="IsSelected"
                                        Value="True">
                                            <Setter
                                            TargetName="Bd"
                                            Property="BorderBrush"
                                            Value="#3399FF" />
                                            <Setter
                                            TargetName="Bd"
                                            Property="BorderThickness"
                                            Value="2" />
                                        </Trigger>
                                        <!-- 鼠标悬停 -->
                                        <Trigger
                                        Property="IsMouseOver"
                                        Value="True">

                                            <Setter
                                            TargetName="Bd"
                                            Property="BorderBrush"
                                            Value="#666666" />

                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                    </Style>

                </ListBox.Resources>

                <ListBox.ItemContainerStyle>
                    <StaticResource ResourceKey="ModListItemStyle" />
                </ListBox.ItemContainerStyle>

                <!-- Mod 名称 -->
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Stretch"
                               Foreground="Black"
                               FontSize="13"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap" />
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>
        </Border>
    </Grid>

</UserControl>
